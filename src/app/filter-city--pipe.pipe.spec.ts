import { FilterCityPipePipe } from './filter-city--pipe.pipe';

describe('FilterCityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
